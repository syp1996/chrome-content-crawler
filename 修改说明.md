# 发送到后台功能修改说明

## 修改概述

已成功修改插件的"发送到后台"按钮功能，现在能够发送markdown格式的数据到后端API，与"保存到本地"功能保持一致的格式。

## 修改内容

### 1. 主要功能文件 (`vue-sidepanel/src/js/sidepanel-global.js`)

**修改位置：** 第336-417行的发送到后台功能

**主要改动：**
- 复用已有的 `domToMarkdown` 函数来生成markdown格式内容
- 在发送的数据中新增 `content.markdown` 字段
- 保留原有的 `content.html` 和 `content.text` 字段以保持向后兼容性
- 添加来源信息 (`来自: ${currentTitle}`) 与保存本地功能保持一致

**新的数据结构：**
```javascript
const apiData = {
    title: currentTitle || '笔记',
    excerpt: plainText.substring(0, 200) + '...',
    content: {
        markdown: markdownContent,  // 新增：markdown格式
        html: doc.body.innerHTML,   // 保留：html格式  
        text: plainText,            // 保留：纯文本格式
        source: currentTitle
    },
    slug: ...,
    status: 0,
    is_pinned: false,
    category_id: 0
};
```

### 2. 测试文件更新

更新了以下测试文件，确保它们也支持新的markdown格式：

- `test-send-to-backend.html` - 完整的发送测试
- `test-mock-backend.html` - 模拟后端测试

两个测试文件都添加了：
- `domToMarkdown` 和 `domChildren` 函数
- 更新了发送逻辑以生成markdown格式

## 支持的Markdown格式

新的实现支持以下HTML到Markdown的转换：

- **标题：** H1-H4 → `#` `##` `###` `####`
- **段落：** `<p>` → 普通段落
- **图片：** `<img>` → `![alt](src)`
- **链接：** `<a>` → `[text](href)`
- **无序列表：** `<ul><li>` → `- item`
- **有序列表：** `<ol><li>` → `1. item`
- **换行：** `<br>` → 两个空格+换行
- **其他标签：** 递归处理子节点

## 向后兼容性

- 保留了原有的 `html` 和 `text` 字段
- API接口保持不变
- 现有的后端系统可以选择使用 `markdown`、`html` 或 `text` 字段

## 使用建议

1. **后端处理：** 建议后端优先使用 `content.markdown` 字段来保存和显示内容
2. **样式保持：** markdown格式能更好地保持文本的结构和样式
3. **兼容性：** 如果后端暂时不支持markdown，仍可使用原有的 `html` 或 `text` 字段

## 测试方法

可以使用以下文件进行测试：
1. `test-send-to-backend.html` - 测试真实的API调用
2. `test-mock-backend.html` - 模拟测试（无需后端服务器）
3. 插件本身的"发送到后台"按钮

## 修改完成

✅ 发送到后台功能现在使用与保存到本地相同的markdown格式
✅ 保持了向后兼容性
✅ 更新了所有相关的测试文件
✅ 无语法错误，可以直接使用
